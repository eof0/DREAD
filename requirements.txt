# Probe Official - Complete Dependencies
# Python 3.12+

# HTTP requests
requests>=2.28.0

# Local web application API
fastapi>=0.115.0
uvicorn>=0.30.0

# DreadAI API route stubs (dreadai/api/app.py)
flask>=2.3.0

# DreadAI agent
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0
# Optional model providers (DREADAI_PROVIDER selects one; installed only if used):
#   local Qwen / Ollama .......... pip install langchain-ollama
#   OpenAI/GPT + any OpenAI-compatible endpoint (OpenRouter, Groq, DeepSeek, Together,
#     xAI, vLLM/LM Studio, ...) .. pip install langchain-openai
#   Google Gemini ................ pip install langchain-google-genai

# HTML parsing
beautifulsoup4>=4.11.0

# PDF report generation
reportlab>=3.6.0

# Templating for HTML reports
jinja2>=3.1.0

# Version comparison for CVE correlation
packaging>=21.0

# YAML configuration support (optional, mostly using Python dicts now)
pyyaml>=6.0

# Additional utilities
urllib3>=1.26.0
certifi>=2022.0.0
